/* global describe, it */

describe('mergeAndDiff', function () {
    describe('#usingTheMergeMethod()', function () {
        it('should return', function (done) {
            done();
        });
    });
});
